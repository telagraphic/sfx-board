{
  "name": "sfx-board",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun run src/server.js",
    "deploy": "bun run src/server.js"
  }
}
